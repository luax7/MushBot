import { Command } from "../Handler";

export default {

    Callback(Message, Args, Client) {
        Message.reply("https://github.com/MushRoom-Paradox/Void-Guild.git")
        return true
    },

} as Command;